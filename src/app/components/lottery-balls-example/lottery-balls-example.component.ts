import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-lottery-balls-example',
  templateUrl: './lottery-balls-example.component.html',
  standalone: true,
  imports: [CommonModule]
})
export class LotteryBallsExampleComponent {
  // Este componente es solo para mostrar ejemplos de las bolas de lotería
}

import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-restriccion-edad',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './restriccion-edad.component.html',
  styleUrl: './restriccion-edad.component.css'
})
export class RestriccionEdadComponent {
  // Podemos agregar lógica específica de la página aquí si es necesario
}

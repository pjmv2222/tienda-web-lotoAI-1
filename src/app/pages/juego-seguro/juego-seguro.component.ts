import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-juego-seguro',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './juego-seguro.component.html',
  styleUrl: './juego-seguro.component.css'
})
export class JuegoSeguroComponent {
  // Podemos agregar lógica específica de la página aquí si es necesario
}
